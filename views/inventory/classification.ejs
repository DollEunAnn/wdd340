<!-- Inventory Classification - heading, message, inventory -->

<!-- heading -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <!-- message -->
        <%# messages() %>

            <!-- inventory items display -->
            <%- grid %>